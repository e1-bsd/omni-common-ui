@import 'main.postcss';

.TopNav {
  align-items: center;
  background-color: var(--colorNavSectionBg);
  box-shadow: var(--headerBarShadow);
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  min-height: 5em;
  position: relative;
}
