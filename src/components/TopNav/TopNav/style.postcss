@import 'main.postcss';

.TopNav {
  align-items: center;
  background-color: var(--colorBase);
  box-shadow: var(--cardShadow);
  display: flex;
  justify-content: space-between;
  min-height: 5em;
  padding: 1em;
}
