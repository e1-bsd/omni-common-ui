@import 'main.postcss';

:root {
  --moduleContainerMarginTop: 20px;
}

.PageCard {
  background: var(--colorCardBg);
  box-shadow: 0 0 3px 1px var(--colorBorder);
  position: relative;
}

.PageCard + .PageCard {
  margin-top: var(--moduleContainerMarginTop);
}
