@import 'main.postcss';

.Card {
  display: block;
  -webkit-overflow-scrolling: touch;
  position: relative;

  &:not(.__backgroundless) {
    @mixin cardbg;
  }

  &:not(.__borderless) {
    @mixin cardshadow;
  }
}
