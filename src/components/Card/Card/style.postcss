@import 'main.postcss';

.Card {
  background: var(--colorBase);
  border: var(--hairThin) solid #c1c1c1;
  display: block;
  overflow: auto;
  -webkit-overflow-scrolling: touch;
  position: relative;
}
