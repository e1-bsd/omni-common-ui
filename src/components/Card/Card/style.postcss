@import 'main.postcss';

.Card {
  background: var(--colorBase);
  border: var(--hairThin) solid #c1c1c1;
  display: block;
  overflow: auto;
  position: relative;
  -webkit-overflow-scrolling: touch;
}
